//First half reverse 
//Samay -->masay
#include<stdio.h>
#include<stdlib.h>
void halfReverse(char *name)
{
	char * word;
	int count=0,offset=0,length=0;
	if(count<0)
	{
		break;
	}
	
	for(length=0;name[length]!='\0';length++);
	word[count]=name[length/2-count];
	
	count++;
	halfReverse(char *name)
	
	}

int main()
{
	char name[5]="Samay";
	halfReverse(name);
	
}
